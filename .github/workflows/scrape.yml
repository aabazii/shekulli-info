name: Scrape Facebook Posts

on:
  schedule:
    - cron: '*/10 * * * *'   # every 10 min (backup to Vercel cron)
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 2

    steps:
      - name: Trigger Vercel scrape
        run: |
          curl -sf -X POST \
            -H "Authorization: Bearer ${{ secrets.ADMIN_PASSWORD }}" \
            https://shekulli.vercel.app/api/scrape \
            | cat
